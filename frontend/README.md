# Life Path Frontend (SvelteKit)

## Setup

1. Install dependencies:
   npm install
2. Start development server:
   npm run dev

Visit: http://localhost:5173

## Environment Variables
- Copy `.env.example` to `.env` and set VITE_API_URL as needed.
